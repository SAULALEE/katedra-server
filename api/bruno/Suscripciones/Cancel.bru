meta {
  name: Cancel
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/suscripciones/me
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{jwt}}
}

docs {
  Sets cancelaAlFinal=true. The account stays PRO until periodoFin, then reverts to FREE on
  the next request that reads the database — no immediate downgrade.
}
